{
  "routes": [
    {
      "route": "/build/*.{data,js,wasm}",
      "headers": {
        "Content-Encoding": "gzip"
      }
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/images/*.{png,jpg,gif}", "/css/*"]
  }
}
